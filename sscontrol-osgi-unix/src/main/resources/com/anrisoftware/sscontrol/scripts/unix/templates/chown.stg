unixChown(args) ::= <<
<args.files:unixChownFile(args);separator="\n">
>>

unixChownFile(file, args) ::= <<
<args.command> <unixChownRecursive(args.recursive)> <args.owner>.<args.ownerGroup> <file>
>>

unixChownRecursive(recursive) ::= <%
<if(recursive)>-R<endif>
%>
